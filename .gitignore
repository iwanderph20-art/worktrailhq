.DS_Store

# Internal key-minting tool — must never be published (holds the unlock secret)
products/integrations/etsy-key-generator.html

# Node dependencies (installed by Netlify at build time)
node_modules/
